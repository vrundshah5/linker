{
  "servers": {
    "sonarqube": {
      "command": "npx",
      "args": ["-y", "sonarqube-mcp-server@latest"],
      "env": {
        // "SONARQUBE_TOKEN": "", // user token
        // "SONARQUBE_URL": "",
        // "SONARQUBE_PROJECT_KEY": ""
      }
    },
    "gitkraken": {
      "command": "uvx",
      "args": [
        "mcp-server-git",
        "--repository",
        // "" // Repo path for the project
      ]
    }
  }
}
